# Nixos Virtual Machine on Parallels Desktp

This repository is about configurations on nixos virtual machine.


## rebuild system

```sh
make rebuild
```

## upgrade via flake

```sh
cd /etc/nixos
sudo nix flake update
```
